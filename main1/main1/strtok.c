#include <stdio.h>
#include <string.h>
int main()
{
     char str[]="This-is-C-Assignment";
     char *token;
     char s[2]="-";
	 int i,j,len;
	 len = strlen(str);
	 

   /* token=strtok(str,s);
   while(token!=NULL)
   {
        printf("%s\n",token);
        token=strtok(NULL,s);
    }
    return 0;*/
}